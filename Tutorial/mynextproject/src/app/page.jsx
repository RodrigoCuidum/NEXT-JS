import Users from "@/src/components/Users";

export const metadata = {
  title: "Mi página Home",
};

const page = () => {
  return (
    <section>
      <Users />
    </section>
  );
};

export default page;
